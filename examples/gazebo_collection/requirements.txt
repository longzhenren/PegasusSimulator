# Gazebo Dynamics Collector Dependencies
# ========================================
#
# Installation:
#   pip install -r requirements.txt
#
# Note: ROS2 and MAVROS must be installed via apt, not pip.
# See README.md for complete installation instructions.
#

# Core dependencies (usually pre-installed with ROS2)
# numpy>=1.20.0
# pyyaml>=5.4

# Optional: ULG log analysis
pyulog>=1.0.0

# Optional: Data analysis
# pandas>=1.3.0
# matplotlib>=3.4.0

# Note: The following are ROS2 packages, install via apt:
# - ros-humble-mavros
# - ros-humble-mavros-extras
# - ros-humble-mavros-msgs
#
# Install command:
#   sudo apt install ros-humble-mavros ros-humble-mavros-extras ros-humble-mavros-msgs
#
# GeographicLib datasets (required by MAVROS):
#   sudo /opt/ros/humble/lib/mavros/install_geographiclib_datasets.sh
